// Template for firebase-messaging-sw.js, generated by scripts/generate-sw.mjs
// (predev/prebuild) substituting VITE_FIREBASE_* env vars. Runs outside the
// Vite module graph, so it can't read import.meta.env directly — the actual
// generated file is gitignored to avoid tripping secret scanners.
importScripts(
  "https://www.gstatic.com/firebasejs/10.7.0/firebase-app-compat.js",
);
importScripts(
  "https://www.gstatic.com/firebasejs/10.7.0/firebase-messaging-compat.js",
);

firebase.initializeApp({
  apiKey: "__VITE_FIREBASE_API_KEY__",
  projectId: "ims-ambulancias",
  storageBucket: "ims-ambulancias.firebasestorage.app",
  messagingSenderId: "562177550205",
  appId: "__VITE_FIREBASE_WEB_APP_ID__",
});

firebase.messaging();
