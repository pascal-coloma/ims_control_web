# Firebase Web App config (Project settings > General > Add app > Web, in
# the ims-ambulancias project). Distinct from the Android app's values.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_WEB_APP_ID=

# Web Push certificate key pair (Project settings > Cloud Messaging > Web Push certificates)
VITE_FIREBASE_VAPID_KEY=
